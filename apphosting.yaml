# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  maxInstances: 1

# Mapeo de variables de entorno y secretos
env:
  # IMPORTANTE: Este secreto DEBE existir en Google Cloud Secret Manager
  # para que el servidor pueda usarlo en tiempo de ejecución.
  - variable: PEDROCASTRO_IMAGENES_GENERA
    secret: PEDROCASTRO_IMAGENES_GENERA
  
  # Variables del repositorio
  - variable: GITHUB_OWNER
    value: "iespedrocastro"
  - variable: GITHUB_REPO
    value: "rayuela-app"
