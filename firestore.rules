rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ACCESO PÚBLICO TOTAL - TRIGGER DE DESPLIEGUE: v1.0.1
    // Permite lectura y escritura a cualquier usuario, autenticado o no.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}