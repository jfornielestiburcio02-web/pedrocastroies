
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite lectura y escritura pública en todos los documentos para facilitar el desarrollo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
