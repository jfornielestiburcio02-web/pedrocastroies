
{
  "entities": {
    "Usuario": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Usuario",
      "type": "object",
      "description": "Entidad de usuario con identificador de sesión manual.",
      "properties": {
        "usuario": {
          "type": "string",
          "description": "Nombre de usuario único."
        },
        "contrasena": {
          "type": "string",
          "description": "Contraseña en texto plano."
        },
        "email": {
          "type": "string",
          "format": "email"
        },
        "nombreCompleto": {
          "type": "string"
        },
        "rolesUsuario": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["EsProfesor", "EsAlumno"]
          }
        },
        "sesion": {
          "type": "string",
          "description": "Identificador de sesión (JSESSIONID)."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "usuario",
        "contrasena",
        "email"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "/usuarios/{usuarioId}": {
      "schema": {
        "$ref": "#/backend/entities/Usuario"
      },
      "description": "Colección de usuarios con gestión de sesión manual."
    }
  }
}
